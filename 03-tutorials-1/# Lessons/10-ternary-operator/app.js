/////////////////////////////////
// unary operator -  typeof: because typeof just have ONLY ONE operand
/////////////////////////////////
let text = 'some text'
// console.log(typeof text); // operand

/////////////////////////////////
// binary operator - because it has 2 operands
/////////////////////////////////
let number = 3
let number2 = 2 + 5 // 2 operands

// ternary operator >>> condition ? (runs if true) : (runs if false)
/////////////////////////////////
const value = 1 < 0
value ? console.log('value is true') : console.log('value is false')

/////////////////////////////////
// if/else
/////////////////////////////////
if (value) {
  console.log('value is true')
} else {
  console.log('value is false')
}
