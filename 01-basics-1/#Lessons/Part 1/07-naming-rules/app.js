/*
  Naming Rules
  - can contain digits, letters, underscore, $
  - must start with letter, $ or _

  - case sensitive 
    -> fullname vs Fullname are diff.
  - camelCase (fullName) or underscore (full_name)

  - NO keyword (class, let, const, new...)
  - CANNOT start with number (12name...)


*/

let full_name = 'random'
console.log(full_name)
